PORT=5000
MONGO_URI=your_mongo_connection_string_here
JWT_SECRET=your_jwt_secret_here
NODE_ENV=production
ADMIN_SECRET=your_admin_secret_here
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Notes:
# - Replace placeholders with real values in your deployment environment.
# - Do NOT commit real secrets to the repository. Add `backend/.env` to .gitignore if not already ignored.
# - FRONTEND_URL is required for CORS to work in production.
# - Rotate exposed secrets immediately after deploying.