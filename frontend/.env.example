# Frontend Environment Variables
# All client-facing variables MUST be prefixed with VITE_

# Backend API URL (set this in each environment)
VITE_API_URL=http://localhost:5000

# Public VAPID key for Web Push Notifications (safe to expose - it's public)
VITE_VAPID_PUBLIC_KEY=your_vapid_public_key_here

